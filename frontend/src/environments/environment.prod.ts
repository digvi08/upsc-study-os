// Overwritten by `npm run build:prod` via scripts/inject-env.mjs
// For local production build: NG_APP_API_URL=http://localhost:8000/api/v1 npm run build:prod
export const environment = {
  production: true,
  apiUrl: '/api/v1',
  googleClientId: '',
  appName: 'AI UPSC Study OS',
};
